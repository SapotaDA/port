# TODO - Vibe coder portfolio

- [x] Refactor `src/App.jsx` to include the vibe-style hero/top section (badge, gradient name, CTA buttons, socials) and keep existing sections (marquee + About/Experience/Projects/Certifications).
- [ ] Update `src/styles/index.css` to add any missing classes referenced by the hero section (`status-badge`, `gradient-text`, `btn-primary`, `btn-outline`, `section-divider`, etc.).
- [ ] Verify build passes (`npm run build`) and fix any CSS/className runtime issues.
- [ ] Create git branch `blackboxai/vibe-portfolio`, commit changes, and push to `https://github.com/SapotaDA/port.git`.

